0. —оздать репозиторий на Github и клонировать его
    —ќ«ƒј¬ј“№ PULL REQUESTS
    master
    Ficha2_hover-effects Ч название ветки
    [Ficha2_hover-effects] add hover effect on button Ч оформление коммита
    Bugfix3_marginsInTable Ч название ветки
    Create pull requests
1. —верстать макет по фигме
2. –еализовать :hover/:active-эффекты (по фигме)
3. –еализовать функциональность добавлени€ задач
    a) «авести объект/массив/экземпл€р ласса
        дл€ хранени€ задач
    b) Ќаписать функцию/методќбъекта дл€ добавлени€
        задачи в список (в переменную)
    c) Ќаписать функцию дл€ отображени€ добавленной
        задачи в HTML
    d) ѕрив€зать обработчик к кнопке добавить
        c.i) ѕолучать значение текста в инпуте
        c.ii) передавать это значение в функцию add
        c.iii) ¬алидацию. —делать кнопку неактивной,
            если значение в инпуте пустое
4. –еализовать функциональность удалени€ записи
    a) ѕройтись по всем элементам списка и прив€зать
        к их кнопкам СxТ обработчик на УкликУ.
        (ѕри созданиии нового элемента в HTML (3.с))
        прив€зывать обработчик
    b) ¬ функции внутри обработчика получать
        индекс/текстовое«начение пол€, найти подобный
        элемент в переменной, где хран€тс€ задачи
    c) ”дал€ть задачу в объекте/массиве/экземпл€ре ласса
    d) ”дал€ть узел задачи в HTML
5. –еализовать сортировку (по аналогии с предыдущем (18/12)
    заданием)
6. ѕротестировать приложение, прогнать код в валидаторе,
    проверить проект на соответствие всем пунктам “«
7.* ¬з€ть дополнительные фичи
*/
const toDos = [
    {СmilkТ: 1},
    {СbreadТ: 2},
    {СteaТ: 3}];
const arrRodos = [СmilkТ, СbreadТ, СteaТ, СmilkТ];
// Application //UserList // User
const add = (text) => {
    let index = toDos.length
    let newTodo = {index: text}
    toDos.push(newTodo)
}